addresses:
  do_not_reply: 'consumr project <do-not-reply@consumrproject.org>'

smtp_pool:
  # use up to 5 parallel connections
  max_connections: 5

  # do not send more than 10 messages per connection
  max_messages: 10
