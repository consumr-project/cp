angular.module('tcp').controller('adminController', [
    '$scope',
    function ($scope) {
        'use strict';

        $scope.loginPopover = {
            showMoreOptions: false
        };
    }
]);
